Metodo IsFim para chegar no fim do programa, senao da EOF

public static boolean isFim(String s){
   return (s.lenght() >= 3 && s.charAt(0) == 'F' && s.charAt(1) == 'I' && s.length(2) == 'M');
}
//Verifica se a palavra FIM é realmente a ultima a ser verificada



resp = resp + (char)(s.charAt(i) + 3)  //Ciframento de Cesar

Modificar no MyIO o tipo de leitura das entradas e saidas . UTF - 8 e 